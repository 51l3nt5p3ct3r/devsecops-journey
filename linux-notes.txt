ls — מציג קבצים בתיקייה הנוכחית  
pwd — מציג את הנתיב הנוכחי  
cd <path> — מעבר בין תיקיות  
mkdir <name> — יצירת תיקייה חדשה  
touch <file> — יצירת קובץ חדש (ב-Windows אפשר גם ni <file>)  
cat <file> — הצגת תוכן של קובץ  
cp <src> <dst> — העתקת קובץ או תיקייה  
mv <src> <dst> — העברת או שינוי שם של קובץ  
rm <file> — מחיקת קובץ  
rm -r <dir> — מחיקת תיקייה  
grep "<text>" <fil

Day 1 Practice:
- תרגלתי יצירת קבצים ותיקיות.
- למדתי איך למצוא ולשנות קבצים דרך ה־Terminal.
- הבנתי איך להשתמש ב־grep כדי למצוא מילים בקובץ.

-------------------------------------
Day 2 – PowerShell Automation

- Cleaned my first PowerShell automation script.
- Learned how to structure output messages with colors and clarity.
- Practiced good version control: updated the script, committed changes, and pushed to GitHub.
- Building habits of documenting every change and maintaining readable, professional code.
